spring:
  cloud:
    config:
      server:
        git:
          uri: ${GITHUB_URL}
          default-label: master
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_PASSWORD}

server:
  port: 8888

encrypt:
  key: ${KEY}
